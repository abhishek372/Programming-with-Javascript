// random method - built-in method comes under Math object
// generates random number between 0 and 0.99

Math.random();

decimal = Math.random(10)
console.log(decimal)

// generate random number between 0 and 10
console.log(decimal*10);
