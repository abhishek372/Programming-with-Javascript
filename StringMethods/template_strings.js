// Strings enclosed within back tick quotes are called as template strings
// It is used to add variable's values through $ operator

let string = `template literal in ES6`;
console.log(string);

// Above can also be written as
console.log(`${string}`);


let time = "06:00";
let date = "12 July, 2022";

// Use of strings to console.
console.log(`I will meet at ${time} on ${date}.`);


// Multiline string
let newString = 'This is a\n multi line string.';
console.log(newString);

// Multi line string using \n character
let p = `
This is also
multi line
string`;

console.log(post);


let post = {
    title: 'JavaScript Template Literals',
    excerpt: 'Introduction to JavaScript template literals in ES6',
    body: 'Content of the post will be here...',
    tags: ['es6', 'template literals', 'javascript']
};


let {title, excerpt, body, tags} = post






