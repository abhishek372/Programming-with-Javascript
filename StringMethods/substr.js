// Using substr() method, we can get substring from the given string
// It takes two arguments, the starting index and number of characters to slice.

let string = "Javascript";
console.log(string.substr());   //by default, substr() method takes starting index as 0 and number of characters as String length

console.log(string.substr(2, 5));