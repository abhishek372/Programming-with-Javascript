// Calling an function for updating properties of object

function myFunc(arr){
    arr[0] = 45;
}

const arr = [30];
console.log(arr);

myFunc(arr);
console.log(arr);

// we can access the properties of object outside the function

