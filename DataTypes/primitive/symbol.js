// Symbol is a new data type introduced in ES6 which is created using Symbol() constructor.

let symbol = Symbol();          // new unique Symbol object
console.log(symbol);
console.log(typeof symbol);